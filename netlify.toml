[build]
  publish = "dist"
  command = "npm run build:react"
  functions = "netlify/functions"

# Daily TMDB → Firestore upcomingAlerts (3:00 UTC)
[functions."check-upcoming"]
  schedule = "0 3 * * *"
